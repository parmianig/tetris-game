// settings.ts
export const GAME_SETTINGS = {
  lockDuringCascade: true,
  gravityCascadeDelayMs: 50, // Slow down block fall in cascade (default: 50ms per step)
  customGravity: 1.0, // User-set gravity (sec per drop) for normal mode
  // add more as needed
};
